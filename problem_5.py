class Solution:
    def longestPalindrome(self, s):
        """
        :type s: str
        :rtype: str
        """
        sLen = len(s)
        centerIndex = int(len(s) / 2)
        if (len(s) % 2 != 0)
            
            
        
sol = Solution()
sol.longestPalindrome('abab');        